export interface Budget {
    budgetName: string ;
    clientName: string;
    webPageService: boolean;
    seoService: boolean;
    googleAdsService: boolean
    totalPrice: number
}

export interface panelInfo {
    numPages: number;
    numLanguage: number;
    totalPrice: number;
}